[package]
name = "amd-ds-common"
edition.workspace = true
publish = false

[dependencies]
reqwest = { version = "0.13.2", features = [
    "blocking",
    "brotli",
    "deflate",
    "gzip",
    "zstd",
] }

scraper.workspace = true

snafu.workspace = true
